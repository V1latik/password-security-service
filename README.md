## 📌 Описание проекта

**Password Security Service** — это микросервис для оценки надёжности паролей, реализованный с использованием FastAPI.  
Сервис предназначен для анализа паролей на соответствие базовым требованиям информационной безопасности и формирования рекомендаций по их улучшению.

Микросервис может использоваться как отдельный компонент в системах аутентификации, управления пользователями или информационной безопасности.

---

## ⚙️ Функциональные возможности

Микросервис выполняет следующие проверки:

- проверка пароля по словарю распространённых и слабых паролей;
- выявление чрезмерного количества повторяющихся символов;
- проверка длины пароля;
- проверка наличия:
  - строчных букв;
  - заглавных букв;
  - цифр;
  - специальных символов;
- расчёт приближённой энтропии пароля;
- формирование итоговой оценки надёжности;
- выдача рекомендаций по повышению безопасности пароля.

---

## 🔐 Меры безопасности

В микросервисе реализованы следующие меры защиты:

- пароль не сохраняется и не записывается в файлы;
- отключено логирование тела HTTP-запросов;
- исключено попадание пароля в сообщения об ошибках;
- обработка выполняется только в оперативной памяти.

---

## 🏗️ Архитектура проекта

password-security-service/
│
├─ app/
│ ├─ main.py # точка входа приложения
│ ├─ api.py # HTTP-интерфейс микросервиса
│ ├─ checker.py # логика анализа паролей
│
├─ common_passwords.txt # словарь распространённых паролей
├─ requirements.txt # зависимости проекта
├─ Dockerfile # файл сборки Docker-контейнера
└─ README.md # описание проекта


---

## 🚀 Запуск проекта локально

### 1. Установка зависимостей

python -m pip install -r requirements.txt
2. Запуск сервиса
python -m uvicorn app.main:app --reload
Сервис будет доступен по адресу:

http://127.0.0.1:8000
Swagger-документация:

http://127.0.0.1:8000/docs
🐳 Запуск в Docker-контейнере
1. Сборка образа
docker build -t password-security-service .
2. Запуск контейнера
docker run -p 8000:8000 password-security-service
Swagger-документация будет доступна по адресу:

http://localhost:8000/docs
📥 Пример запроса
{
  "password": "P@ssw0rd123"
}
📤 Пример ответа
{
  "уровень_надежности": "сильный",
  "оценка": 5,
  "энтропия": 52.3,
  "рекомендации": []
}
📚 Используемые технологии
Python 3.12

FastAPI

Uvicorn

Docker
